{
  "name": "direct-diffusion",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode development",
    "dev:integration": "vite --mode integration",
    "build": "vite build",
    "build:integration": "vite build --mode integration",
    "preview": "vite preview",
    "rules:deploy:prod": "firebase deploy --only firestore:rules --project prod",
    "rules:deploy:integration": "firebase deploy --only firestore:rules --project integration",
    "rules:deploy:dev": "firebase deploy --only firestore:rules --project dev"
  },
  "dependencies": {
    "firebase": "^12.12.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.6"
  }
}
